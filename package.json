{
  "name": "chatify-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix frontend --legacy-peer-deps --include=dev && npm run build --prefix frontend && npm install --prefix backend --legacy-peer-deps",
    "start": "node backend/src/index.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  }
}
